# 🧹 Интерактивная анкета клининговой компании

Современная веб-анкета для сбора информации от клиентов клининговой компании с красивым интерфейсом и удобной навигацией.

## ✨ Особенности

- 📱 **Адаптивный дизайн** - отлично работает на всех устройствах
- 🎨 **Современный UI** - градиенты, анимации, плавные переходы
- 💾 **Сохранение данных** - информация сохраняется при переходе между блоками
- 🚀 **Быстрая загрузка** - никаких зависимостей, только CDN
- 📊 **Прогресс-бар** - визуальное отображение заполнения анкеты
- 🎯 **10 тематических блоков** - логичное разделение информации

## 📋 Структура анкеты

1. **Контактные данные** - имя, адрес, телефон, Telegram
2. **Об объекте** - тип жилья, площадь, комнаты
3. **Жильцы** - дети, животные, аллергии
4. **Тип уборки** - вид уборки и удобное время
5. **Приоритетные зоны** - важные места для уборки
6. **Ограничения** - что нельзя трогать
7. **Дополнительные услуги** - чистка матрасов, мебели и т.д.
8. **Подарки** - желание получать бонусы
9. **Комментарии** - дополнительные пожелания
10. **Подтверждение** - согласие и подпись

## 🚀 Быстрый старт

### Вариант 1: Простой запуск (без установки)

Просто откройте файл `index.html` в любом браузере:

```bash
# Windows
start index.html

# macOS
open index.html

# Linux
xdg-open index.html
```

### Вариант 2: Локальный сервер

Если хотите запустить через локальный сервер:

```bash
# Установите зависимости
npm install

# Запустите сервер
npm start
```

Откройте браузер: `http://localhost:8080`

## 📦 Установка зависимостей

```bash
npm install
```

## 🛠️ Доступные команды

```bash
# Запуск локального сервера разработки
npm start

# Запуск production сервера
npm run serve
```

## 📁 Структура проекта

```
cleaning-questionnaire/
├── index.html          # Основной файл анкеты
├── README.md           # Документация
├── package.json        # Конфигурация проекта
└── .gitignore         # Игнорируемые файлы
```

## 🎨 Технологии

- **HTML5** - разметка
- **JavaScript (Vanilla)** - логика приложения
- **Tailwind CSS** (CDN) - стилизация
- **Lucide Icons** - иконки (эмодзи)

## 💡 Использование

1. Клиент открывает анкету
2. Заполняет данные по блокам
3. Может переходить между блоками используя:
   - Кнопки "Назад" / "Далее"
   - Цифровую навигацию внизу
4. Данные автоматически сохраняются
5. На последнем блоке нажимает "Завершить"
6. Данные отправляются (в текущей версии показываются в alert)

## 🔧 Настройка

### Изменение цветов

Откройте `index.html` и измените классы Tailwind:

```html
<!-- Основной градиент -->
from-purple-600 via-pink-600 to-blue-600

<!-- Кнопки -->
from-purple-500 to-pink-500
```

### Добавление новых вопросов

В массиве `blocks` добавьте новый блок:

```javascript
{
    number: 11,
    title: '📝 Новый блок',
    render: () => `
        <h2>Заголовок</h2>
        <!-- Ваш HTML -->
    `
}
```

### Отправка данных на сервер

Замените alert в конце на fetch запрос:

```javascript
// Вместо alert
fetch('/api/submit', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(formData)
})
.then(response => response.json())
.then(data => alert('Успешно отправлено!'));
```

## 📱 Браузеры

Поддерживаются все современные браузеры:

- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+

## 🤝 Вклад в проект

Если хотите улучшить проект:

1. Форкните репозиторий
2. Создайте ветку (`git checkout -b feature/improvement`)
3. Закоммитьте изменения (`git commit -am 'Add new feature'`)
4. Запушьте ветку (`git push origin feature/improvement`)
5. Создайте Pull Request

## 📄 Лицензия

MIT License - используйте свободно!

## 📞 Контакты

Если есть вопросы или предложения - создайте Issue в репозитории.

---

**Создано с ❤️ для клининговых компаний**
